# 本地重构版

本版经本地预览确认后同步至主仓库。旧 css/style.css 与原有图片保留；新页面不再加载旧样式或分析追踪脚本。生成产物随源码提交，静态托管可直接使用仓库根目录，无需安装依赖。

## 选择这个架构的原因

个人主页以内容和导航为主，不需要 React 水合、后端服务器或庞大的依赖树。使用 Node 内置模块做一次静态生成，浏览器拿到完整 HTML；CSS 合并为单个文件，交互使用原生 ES Modules。无 npm 安装步骤、无运行时框架、无远程字体、无 CDN 请求。

不是大型应用框架：以后如果增加账户、后台或复杂路由，可以再迁移到 Astro 等框架；当前内容数据和设计变量已经独立，迁移不需要从巨型 HTML 中重新抽取内容。

## 本地启动

需要 Node.js 20+；预览使用 Python 3。

```sh
cd xiaowu-homepage
node scripts/build.mjs
node scripts/check.mjs
python3 -m http.server 8000 --bind 127.0.0.1
```

打开 http://127.0.0.1:8000/ 。不要直接通过 file:// 打开，ES Modules 需要 HTTP 服务。
服务仅监听本机，手机上的 localhost 不会访问这台电脑。

## 修改入口

| 文件 | 用途 |
| --- | --- |
| src/data.mjs | 个人信息、6 个社交入口、7 个项目；增加项目只需添加数据 |
| src/head.html | SEO、canonical、Open Graph 和 JSON-LD 元数据 |
| scripts/build.mjs | 页面模板、静态 HTML 生成与 CSS 合并 |
| css/tokens.css | 明暗主题颜色、字体及基础设计变量 |
| css/base.css | Reset、布局基础、减少动效偏好 |
| css/components.css | 响应式页面组件样式 |
| js/theme.js | 主题切换与本地持久化 |
| js/main.js | 项目筛选、原生 dialog、复制和二维码按需加载 |
| scripts/check.mjs | 可重复运行的内容与构建检查 |

`index.html` 和 `css/main.css` 是生成产物；修改源文件后运行 `node scripts/build.mjs`，然后刷新浏览器。直接修改生成产物会在下一次构建时被覆盖。修改个人 SEO 信息时也需同步 src/head.html；它仍保留独立的结构化数据。

## 交互与降级

- 默认跟随系统主题；手动切换后保存在 localStorage。浏览器禁止存储时仍能切换，仅不持久化。
- 无 JavaScript 时所有项目与真实外链仍可阅读使用，筛选/主题按钮隐藏，BTC 完整地址通过 noscript 显示。
- 公众号原站没有二维码/有效落地链接；这里显示微信搜索说明并支持复制名称，没有伪造二维码。
- BTC 二维码库仅打开弹窗时从本地 public/vendor 加载。生成失败时保留完整地址。
- 复制依赖浏览器剪贴板权限及安全上下文（localhost 或 HTTPS）。失败时提供手动复制提示。
- dialog 支持 Escape 关闭、关闭按钮和点击遮罩；使用浏览器原生焦点管理。
- 卡片上的图形和电子书封面为本次设计的装饰，不是实际产品截图或官方书封。

## 验证边界

静态检查覆盖：7 个项目和 URL、6 个社交入口、2/2/3 分类、静态资源存在性、SEO JSON-LD、无远程运行时脚本、CSS 合并和 BTC 二维码生成。

浏览器 DOM 逻辑检查通过：项目分类结果、深色主题状态与计算颜色、头像和图标加载、仅本地资源请求。320、390、768、1280 像素宽度的页面均无横向溢出，项目网格和社交网格按断点切换；这是计算布局检查，不是视觉或真机验收。自动化真实点击曾超时，键盘操作未产生预期效果，不能把脚本触发点击视为真实点击验收。复制权限、真实手机触控和视觉效果仍需人工确认；未进行 Lighthouse 或真实网络速度测量。

本版不加载原站统计脚本。robots.txt、sitemap.xml、分享图和 _redirects 保持原样。线上发布是否自动触发，取决于托管平台与主分支的绑定配置。
